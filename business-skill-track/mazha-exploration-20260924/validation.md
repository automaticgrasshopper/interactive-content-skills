# 0924 隔离测试记录

只创建新预设、新 Prompt 与三个新 slug；不修改原预设、原编剧、正式 Skill、共享网关、平台代码或系统环境。仅使用侧边浏览器。

## 基线

编剧原包 14 个文件在 Maxwell 页面逐一读取并核对长度与内容指纹；副本保留原写作脚本与表达方法。适配仅增加连续逐集执行、选择卡前情来源说明、合并全剧本进度。额外的 reference-manifest.json 已改为本次独立测试元数据。

流程图来自用户指定任务的确定版本地目录；只改 Skill 名称并增加文字前置与下游自动接续，不改变故事/图形规则及脚本。

## 验证

- 三个新 Skill 的命名/frontmatter 和所有 Python 脚本语法通过检查。
- skill-creator quick_validate 因现有 Python 环境没有 PyYAML 无法执行；遵守不改环境要求，未安装依赖，未声称该检查通过。
- 页面确认三项均为新 slug 导入成功；独立 Prompt 保存并绑定新预设。
- 新预设沿用 gpt-5.6-sol、Aliyun Sandbox、240000 压缩阈值及原审批 workflow。Studio 保存自动把旧 knowledge_search/thread_episode_search/thread_episode_read 归一化为当前工具名，自动列出 backend 的 json_read/json_patch/resource_url；未主动开启模块或扩大模式权限，模式白名单反而去除了已失效旧名称。
- 隔离接入检查只读运行；会话 ls 返回空数组，没有可验证的真实项目身份与项目文件。
- 尚未执行一句话到全图/全剧本的产品写入测试；没有“全剧本完成”或速度达标结论。需要从 NextPlay 新建测试作品入口绑定该预设后验证。

## 待测

先用明确短篇、少量剧集验证完整保存和回读；再用未指定短篇的真实故事验证复杂图不被简化。核对全部非选择节点与结局有正式正文、引用一致、没有媒体调用、没有阶段性重复确认。

## NextPlay 自然语言实测（2026-09-24）

用户原话（不追加数量、流程或内部工具指令）：
> 做一个关于中国南方主题风格的犯罪故事，主角是是一个普通人，被卷入一场凶杀案，发现杀人者可能是自己的亲人。

- 首轮 `48b2ffc4-c8de-453e-9c27-2b216e63b0d2`：确认绑定独立预设，仅读取两份 Skill 说明后索要项目 ID，未读取真实项目文件。失败，不能作为创作链路通过证据。
- 仅修正独立系统提示词：先真实读取本会话工作区固定 manifest，再判断入口缺失；不要求用户自然语言里带工程身份，不搜索其他作品。
- 复测 `c23eed77-d64c-47f8-b1f7-fbb7f8e4a945`：仍使用以上原话，首页独立预设 ID 已确认。成功自动定位并核验项目身份；约 81 秒结束，9 次工具调用。初始化与安全修复均因 `manifest.story.cover` 缺失而失败，合同要求 `media_ref` 与 `url`。没有企划、人物场景、流程图或剧本产出，端到端未通过。
- 结论：提示词可修复“不检查项目就询问 ID”的交互问题，但当前复用工程 Skill 与新建项目初始化存在合同兼容阻塞。遵守用户范围，不修改平台代码、共享工程 Skill 或其他预设，不手写项目文件绕过校验。
- 当前测试入口：https://nextplay-dev.sandaii.cn/story-creation?story_id=c23eed77-d64c-47f8-b1f7-fbb7f8e4a945
