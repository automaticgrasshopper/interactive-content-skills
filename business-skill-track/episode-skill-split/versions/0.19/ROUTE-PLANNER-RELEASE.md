# 流程图规划师 0.19

恢复完整分支规划、全路径记账和 A/B 复检，保留已拆分的编剧职责与 handoff.v1 对外字段。本版从线上 0.18 测试副本继承业务接口、数量解析、正式投影与旧缓存恢复代码；主线和分支规划参考旧 0.46 / 0.52。

- 新任务：八项上游冻结 → 废弃形状探索（不入门禁）→ 正式拓扑修订链 → 路线状态记账 → A/B → 梗概投影与语义复检 → 独立情绪脊 → PLANNING_ACCEPTED。
- 拓扑可整体前瞻规划，无 FIFO / 80:20 抽签；稳定临时ID经确定性投影编号。旧 growth-state 缓存仍走 0.18 恢复协议，不转换历史。
- 用户数量、题材与禁区优先；四类结局只分类，不设最低数。逐路径状态前提检查不把回汇后的差异清零。
- 梗概接口只接受正文、冲突与停止位置，不能改状态、边或选择；语义复检明确检查选项提前执行。
- A/B只绑定上游与拓扑记账；情绪脊独立，最终回执最后生成。局部修订保留旧图，自动归档下游。只改梗概或情绪不回写拓扑。

状态所有权：作者提交规划与语义审查；topology_plan.py独占写入正式内部拓扑修订与投影；planned_gate.py核验 A/B、梗概与状态证据；planning_gate.py签发最终规划回执；accept_route.py沿用正式路线封存。编剧只能消费既有路线材料，未改其文件或线上资源。

兼容边界：对外字段、capability_id和正式封存不变；新增内部计划合同。原始上游八项在初次冻结后不自动重写。语义判断仍由模型完成，脚本只能校验显式事实与证据绑定；同一执行者的两遍审查不等于独立评审。全部路径超过10000时报错，不默默截断。

本地验证：17项新规划行为契约测试、14项旧缓存局部修复回归与skill-creator quick_validate通过。测试夹具中的PASS仅验证工程合同，不代表故事质量；线上自然案例另行记录。

测试资源：episode-route-designer-biz / 06e9a31d-b667-4124-948b-fcc2e79accf0，保留显示名“蚂蚱-分支-0907新测试”。使用预设“蚂蚱的测试”默认配置。该Skill还被另外两个测试预设引用，本次只替换指定Skill。正式资源、预设、编剧Skill未改，也未解锁。

管理员事项：本PR仅合入源版本，不代表正式Maxwell发布。正式Skill同步需要用户另行授权并经管理员发版流程，不得解锁预设或自动同步正式资源。

首轮线上发现B回执可用节点标题冒充因果证据，已新增正文/冲突/停止边界引文限制与对应回归。用户随后明确授权同一测试Skill上传修复；平台代码、预设与模型配置不变。
