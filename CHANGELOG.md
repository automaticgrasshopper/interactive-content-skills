# Changelog

## 2026-08-01

- 业务协作版新增`0.07`独立快照，通用内核仍为`v0.1.53`，并完整继承`0.06`选择单一事实源与全部既有门禁。
- 新增对外消息防火墙：默认不发送过程消息；平台无法静默时仅以`正在生成中`作为降级占位。合同、拓扑、门禁、冷读、复检、节点去重和修复等内部状态不得出现在用户消息中，技术 UI 同步丢弃非白名单过程状态。
- 业务协作版新增`0.06`独立快照，通用内核仍为`v0.1.53`；不改变情绪脊、拓扑、逐集写作、台词复写、冷读、角色首次出场、十一字段或用户意图合同。
- 修正选择节点的交付边界：`分集列表[].互动节点`成为唯一业务事实源，`分集结构`只保存剧集节点和真实后继；前端仍按`选择数`与互动字段派生可视选择节点，但业务 JSON 不再重复输出`choice-xxx`。
- 新增强拒绝门禁：`分集结构`出现`choice-xxx`、`节点类型=选择节点`或重复互动内容即失败；旧`0.05`双份选择输出已作为负例确认无法通过。
- 通用`episode-generator`升级为`v0.1.53`，业务协作版新增`0.05`独立快照；只新增用户意图内部合同与履约门禁，既有生成、拓扑、字段、台词、角色首次出场和组装流程保持不变。
- 用户明确要求写入私有`nextplay.user-intent-lock.v1`合同，并由独立履约复检逐项举证；用户要求源、合同和最终拓扑／正文摘要共同绑定，合同摘要同时绑定逐集复检回执。
- 用户前后明确要求无法同时成立时返回`ASK_USER_REQUIRED`并启动 AskUser；用户解决前不得生成情绪脊、拓扑或正文。合同、冲突、证据和摘要不进入数据库、公开文件、业务 JSON 或玩家界面。
- 通用`episode-generator`升级为`v0.1.52`，业务协作版新增`0.04`独立快照；保留`v0.1.51/0.03`不变。
- 正式分集对象由十字段升级为十一字段，新增`选择数`；`分集数`只统计剧集节点，`选择数`统计独立选择节点，上游建议集数只约束剧集节点。
- 采用创作拓扑与交付拓扑双层模型：情绪脊、剧本与资产只绑定剧集节点；业务`分集结构`确定性展开`剧集 -> 选择 -> 目标剧集`，选择节点不得成为入口或携带剧本、资产、情绪坐标。
- 增加对外输出防火墙，用户可见进度不再暴露冷读、情绪脊、拓扑推导、哈希、门禁、回执或缓存信息。
- 固化创作优先级：用户最新明确要求高于上游建议和Skill启发式；除安全、正式合同或客观不可实现外，不得以“重新拓扑”为由删除或替换用户内容。

## 2026-07-30

- 将通用 `episode-generator` 完整升级并记录为 `v0.1.51`：保留原生成流程，强化情绪脊合同、拓扑、结局数量、资产与角色首次出场、逐集冷读回执、局部修改审计和三文件事务组装。
- 建立独立业务协作版本轨 `episode-generator_biz`，保留 `0.01`、`0.02`、`0.03` 快照；当前 `0.03` 以通用 `v0.1.51` 为完整业务内核，只增加工程接口与业务 JSON 适配。
- 正式分集对象由九字段升级为十字段，新增 `分集数`；其值为冻结拓扑后的实际节点总数，必须在全部分集对象中一致，并由通用组装器和业务校验器强验证。
- 明确大纲中的剧情节点总数建议是可选软区间：存在时用于拓扑体量校准，缺失时不得阻塞；仅当用户明确要求固定集数时才作为硬约束。
- 公开文件固定为 `episode-flowchart.svg`、`episode-structure.md`、`episode-script.md`；缓存、哈希、回执、内部摘要和 Reference 不进入玩家可见目录或业务返回。

## 2026-07-24

- 从 `interactive-drama-lab` 完整提取受 Git 管理的 11 个正式 Skill。
- 保留 `skill/` 的相关提交历史。
- 建立统一的 `skills/` 可安装目录。
- 将 `episode-generator` 独立仓并入统一 Skill 仓。
- 建立 Skill 与互动节奏平台互不读取、互不自动同步的维护边界。
