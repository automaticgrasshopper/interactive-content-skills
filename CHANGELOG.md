# Changelog

## 2026-07-30

- 将通用 `episode-generator` 完整升级并记录为 `v0.1.51`：保留原生成流程，强化情绪脊合同、拓扑、结局数量、资产与角色首次出场、逐集冷读回执、局部修改审计和三文件事务组装。
- 建立独立业务协作版本轨 `episode-generator_biz`，保留 `0.01`、`0.02`、`0.03` 快照；当前 `0.03` 以通用 `v0.1.51` 为完整业务内核，只增加工程接口与业务 JSON 适配。
- 正式分集对象由九字段升级为十字段，新增 `分集数`；其值为冻结拓扑后的实际节点总数，必须在全部分集对象中一致，并由通用组装器和业务校验器强验证。
- 明确大纲中的剧情节点总数建议是可选软区间：存在时用于拓扑体量校准，缺失时不得阻塞；仅当用户明确要求固定集数时才作为硬约束。
- 公开文件固定为 `episode-flowchart.svg`、`episode-structure.md`、`episode-script.md`；缓存、哈希、回执、内部摘要和 Reference 不进入玩家可见目录或业务返回。

## 2026-07-24

- 从 `interactive-drama-lab` 完整提取受 Git 管理的 11 个正式 Skill。
- 保留 `skill/` 的相关提交历史。
- 建立统一的 `skills/` 可安装目录。
- 将 `episode-generator` 独立仓并入统一 Skill 仓。
- 建立 Skill 与互动节奏平台互不读取、互不自动同步的维护边界。
